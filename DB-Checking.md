

netstat -nltp: 
Finds what port a process is listening on. It shows active TCP network connections, displaying numerical addresses and the specific Process ID (PID) or program name using each port.ps -ef |

 grep mysqld:
  Finds if a process is running. It lists every active process on the system and filters the results to show details (like the PID, owner, and start time) specifically for mysqld.